#数据库连接信息
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ssm?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
  redis:
    host: 127.0.0.1
    port: 6379
#配置mybatis的信息
mybatis:
  type-aliases-package: com.movle.domain
  #加载mybatis映射文件
  mapper-locations: classpath:mapper/*Mapper.xml

